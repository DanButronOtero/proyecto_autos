<%- include('../../templates/header'); -%>
    <!-- Banner Area -->
    <div class="banner-item banner-1 steller-parallax" data-stellar-background-ratio="0.5" style="height: 10em;">

    </div>

    <!-- Banner Area End -->
    <div class="section section-padding more-feature-section style-2" data-stellar-background-ratio="0.5">
        <div class="container steller-parallax">
            <h1>VENTAS</h1>
            <table class="table">
                <thead class="thead-light">
                    <th>id</th>
                    <th>fecha</th>
                    <th>id_coche</th>
                    <th>id_vendedor</th>
                    <th>id_cliente</th>
                    <th>estado_logico</th>
                    <th>editar</th>
                    <th>borrar</th>
                </thead>
                <% for (let i = 0; i < registros.length; i++) { %>
                    <thead>
                        <th>
                            <%= registros[i].id %>
                        </th>
                        <th>
                            <%= registros[i].fecha %>
                        </th>
                        <th>
                            <%= registros[i].id_coche %>
                        </th>
                        <th>
                            <%= registros[i].id_vendedor %>
                        </th>
                        <th>
                            <%= registros[i].id_cliente %>
                        </th>
                        <th>
                            <%= registros[i].estado_logico%>
                        </th>
                        <th>
                            <form action="/ventas/update" method="GET">
                                <button type="submit" class="btn btn-primary" aria-label="Left Align">
                            <input type="hidden" name="id" value="<%=registros[i].id  %> ">
                            <span class="glyphicon glyphicon-pencil" aria-hidden="true"></span>
                          </button>
                            </form>

                        </th>
                        <th>
                            <form action="/ventas/delete" method="post">
                                <button type="submit" class="btn btn-primary" aria-label="Left Align">
                            <input type="hidden" name="id" value="<%=registros[i].id  %> ">
                            <span class="glyphicon glyphicon-trash" aria-hidden="true"></span>
                          </button>
                            </form>
                        </th>

                    </thead>

                    <% } %>
            </table>
            <div>
                <a class="btn btn-primary" href="/ventas/create" role="button">
                    <span class="glyphicon glyphicon-floppy-disk" aria-hidden="true"> REGISTRAR</span>
                </a>

            </div>
        </div>
    </div>
    <%- include('../../templates/footer'); -%>